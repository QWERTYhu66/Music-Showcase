// Backend API base URL for authentication endpoints.
// For now this points at the existing music-showcase-server backend.
window.MS_API_BASE = window.MS_API_BASE || 'https://music-showcase-server.onrender.com';

